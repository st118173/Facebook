<br><br>


<%= form_for(fb) do |f| %>
    <% if fb.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(fb.errors.count, "error") %> prohibited this fb from being saved:</h2>

          <ul>
            <% fb.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="field">
      <%= f.label :email %>
      <%= f.text_field :email %>
    </div>

    <div class="field">
      <%= f.label :password %>
      <%= f.text_field :password %>
    </div>



    <div class="actions">
      <%= f.submit %>
    </div>
<% end %>

<%= form_for(fb) do |f| %>
    <div class="field">
      <%= f.label :firstname %>
      <%= f.text_field :firstname %>
    </div>

    <div class="field">
      <%= f.label :surname %>
      <%= f.text_field :surname %>
    </div>

    <div class="field">
      <%= f.label :mobile %>
      <%= f.number_field :mobile %>
    </div>

    <div class="field">
      <%= f.label :newpassword %>
      <%= f.text_field :newpassword %>
    </div>

    <div class="field">
      <%= f.label :dob %>
      <%= f.text_field :dob %>
    </div>

    <div class="field">
      <%= f.label :gender %>
      <%= f.check_box :gender %>
    </div>
    <div class="actions">
      <%= f.submit %>
    </div>
<% end %>